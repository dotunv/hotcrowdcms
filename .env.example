# HotCrowd CMS Environment Variables

# Postgres (Neon) — used only by apps/api
# DATABASE_URL=postgresql://USER:PASSWORD@ep-xxx.region.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=

SECRET_KEY=change-me

# Absolute origin the player uses for media URLs
PUBLIC_API_URL=http://127.0.0.1:8000

# Player CORS (CMS uses Next.js same-origin rewrites)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173,https://hotcrowd-web-player.vercel.app

COOKIE_SECURE=false

# Optional Cloudflare R2 later; local disk is the default
USE_S3=False
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_ENDPOINT_URL=
R2_CUSTOM_DOMAIN=
